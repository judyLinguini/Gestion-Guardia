1. **Grados seleccionables (Point 1)**: Modificar `index.html` para que el campo "Grado Militar" sea un `<select>` con las opciones Sdo., Cabo 2ª, Cabo 1ª, Sgto. 2ª, Sgto. 1ª.
2. **Eliminar contadores manuales y adaptar a historial (Point 9 y 7)**: Quitar los campos de `guardiasHabiles` y `guardiasFinde` del HTML y del JS. La lógica de generación se basará en un historial de guardias (calculado o guardado globalmente) para balancear la cantidad total de guardias, permitiendo así que quienes no hagan en días hábiles compensen en fines de semana al tener menos guardias totales.
3. **Módulos colapsables (Point 5)**: Modificar los `<div class="card">` para incluir botones en el `card-header` que activen el colapso (`data-bs-toggle="collapse"`) del `card-body`.
4. **Licencias y días específicos fraccionados (Points 4 y 8)**: En el formulario de personal, agregar la funcionalidad para añadir múltiples periodos de licencia y múltiples fechas específicas de no disponibilidad, mostrando las listas debajo de los inputs antes de guardar.
5. **Ordenación por grado y drag and drop (Point 6)**: Asignar un peso numérico a los grados. Al guardar, ordenar siempre por grado. Implementar drag and drop en las filas de la tabla de personal (`draggable="true"`) para que, al reordenar dentro de un mismo grado, se actualice un campo `ordenInterno` en los objetos de personal y se persista.
6. **Guardias de a dos personas y lógica de tiempo (Points 2 y 3)**: Modificar `generarGuardias` para que asigne 2 personas por día. La selección ordenará a los candidatos primero por la menor cantidad de guardias totales y luego por la mayor cantidad de días desde su última guardia (para espaciarlas ~20 días).
7. **Modificación manual de guardias (Point 10)**: En la tabla de guardias generadas, agregar un botón para modificar los asignados (abriendo un pequeño modal o cambiando el texto por dropdowns) y guardar los cambios.
8. **Registro de acciones (Point 11)**: Crear una función `registrarAccion(mensaje)` que guarde eventos en un arreglo en `localStorage`. Agregar un botón en la pestaña de Importar/Exportar para descargar este log.
9. **Pre-commit y submit**: Ejecutar los pasos de validación e incorporar todos los cambios al commit final.
